# 数据集扩展规划 v3(最终决策版)

> 状态: **已定稿待实施**
> 日期: 2026-08-27

---

## 0. 决策记录(逐条)

| # | 决策 | 落实 |
|---|------|------|
| 1 | 室外距离拉长 | 室外 12–30 m;室内沿用 4–28 m |
| 2 | 边界不统一,室内室外不同边界 | 室内 = 仓库 region;室外 = 工业大区域(两套 region) |
| 3 | 室外高空覆盖大区域,室内低空按现有场景 | 室外 ±25m 大区域;室内沿用仓库 [-7,10]×[0,30] |
| 4 | 场景数没问题 | 每层 8/8/14/14(类 A、类 B 各 44 场景) |
| 5 | 高度均匀随机 | 类 A ∈[0.5,2.0],类 B ∈[10,13],均匀分布 |
| 6 | 障碍高度有高有低随机,但高于作业高度 | 类 A ∈[3,6]m,类 B ∈[14,20]m,均 > 作业高度 |
| 7 | 高空提升配额 | 类 B 提高 NORMAL/TURN 目标 |

---

## 1. 两类场景定义

### 类 A — 室内 + 室外低空(作业层 0.5–2.0 m)
- 障碍半径 0.1–3.0 m;四层级:
  - small 0.1–0.5 / medium 0.5–1.5 / large 1.5–3.0 / mixed 0.1–3.0
- 密度(稀疏→稠密): 0.05–0.08 / 0.09–0.13 / 0.12–0.18 / 0.12–0.16
- 距离: 室内 4–28 m,室外低空 12–30 m
- 障碍高度随机 [3,6] m(> 作业高度,贯穿作业层)

### 类 B — 室外高空(作业层 10–13 m)
- 障碍半径 1.0–6.0 m;四层级:
  - small 1.0–2.0 / medium 2.0–3.5 / large 3.5–6.0 / mixed 1.0–6.0
- 密度(稀疏→稠密): 0.05–0.08 / 0.09–0.13 / 0.12–0.18 / 0.12–0.16
- 距离: 12–30 m
- 障碍高度随机 [14,20] m(> 作业高度,贯穿作业层)

---

## 2. Region(边界不统一,两套)

| 区域 | region (min_x,max_x,min_y,max_y) | Unity 场景 |
|------|----------------------------------|-----------|
| 室内(仓库) | `[-7, 10, 0, 30]`(沿用) | 1 WAREHOUSE |
| 室外(工业) | `[-25, 25, -25, 25]`(大区域,待点云确认) | 0 INDUSTRIAL |

- 室内低空完全沿用现有场景选择与仓库 region。
- 室外高空/低空覆盖工业大区域,起终点与障碍生成在此区域内;
  已知障碍(点云固定结构)作普通障碍避让。

**实现方式(两套 region)**:
- 室内、室外各用一份 config(region 独立,专家各自 configure)。
- 室内沿用 `il_dataset_joint_v2_config.yaml`(region 不变,只加高度随机
  与障碍高度随机)。
- 室外新建 `il_dataset_outdoor_config.yaml`(region ±25m、作业层、障碍参数)。
- 两 run 采集输出合并到同一目录 `il_data_joint_v2_col_5`
  (manifest 追加/合并,不改 scene_id 冲突)。

---

## 3. 高度与障碍(2.5D)

- 每任务随机作业高度(均匀),起终点同高:
  - 类 A: flight_height_m ~ U[0.5, 2.0]
  - 类 B: flight_height_m ~ U[10, 13]
- 障碍为 scene 级竖直圆柱,中心 z = 该类的作业层中心(类 A 1.25m、类 B 11.5m),
  高度随机但 > 作业高度(类 A [3,6]m、类 B [14,20]m)。
  → 层内任意高度任务看到相同水平截面(半径 r),2.5D 成立。

---

## 4. 已知障碍(点云固定结构)= 普通障碍

- 场景 0/1 点云按作业层切片 → 2D 占据图 → 普通障碍格:
  - 障碍生成避开、任务起终点避开、A* 连通检查计入。
  - 专家经深度观测自然绕行,无特殊语义。
- 室内仓库点云(如无固定障碍)可跳过此步。

---

## 5. 配额与观测

- 专家观测不变: fov 90°、range 5m、resolution 0.1m。
- 距离分层: short ≤8m / medium ≤16m / long >16m(室内沿用;
  室外因 12–30m 整体偏长,long 占比自然升高)。
- **高空配额提升**: 类 B 的 NORMAL/TURN 目标在现有基础上提高
  (如 macro:normal 256→384、turn 96→144,具体按试点校准)。

---

## 6. 数据集合并

- 单一目录 `il_data_joint_v2_col_5`:
  - 类 A 室内(仓库 scene 1)+ 类 A 室外低空(工业 scene 0)
  - 类 B 室外高空(工业 scene 0)
- manifest 记录每 task: `flight_height_m`、`region_class`(indoor/
  outdoor_low/outdoor_high)、`level`、`distance_class`。

---

## 7. 分阶段实施

1. **阶段 0 — 点云基准化**: 导出 scene 0/1 点云 → 按作业层切片 → 占据图,
   俯视图确认室外 region 边界与固定结构位置。
2. **阶段 1 — 生成器扩展**: (a) per-task 随机作业高度; (b) 障碍随机高度
   (> 作业高度); (c) 已知障碍占据图避让; (d) 室外大 region + 子区域采样;
   (e) 距离 12–30m。单元验证。
3. **阶段 2 — 类 A 试点**: 室内 + 室外低空各小规模,blueprint 预检 + 采集。
4. **阶段 3 — 类 B 高空试点**: 1–6m 障碍 + 10–13m 飞行 + 深度可见性单点
   验证 + 小规模采集 + 配额校准。
5. **阶段 4 — 全量合并**: 两类 4 层级全量 + top-up → `col_5`。

---

## 8. 实施前最后确认(可选,可直接进入阶段 0)

- 室外 region 建议 `[-25,25]×[-25,25]`,是否需按点云固定结构进一步裁剪
  (阶段 0 俯视图出来后定)。
- 类 B 配额提升幅度(先按 1.5× 试点)。
- 两 run 合并方式: 追加 manifest 到同一目录 vs 分别目录 + 后合并(建议后者,
  避免 `_clear_collection_output` 误删)。
