# 数据集扩展规划 v2 — 两类场景(室内+室外低空 / 室外高空)

> 状态: **待审核(修订版)**
> 日期: 2026-08-27
> 已按最新决策修订: 两类各 4 层级、高空障碍 1–6m、高度随机定高 2.5D、
> 已知障碍当普通障碍、起点区域内、障碍生成边界内缩、长墙暂不生成、
> 专家观测不变、长中短距离、合并一个数据集。

---

## 1. 两类场景定义(每类 4 层级)

### 类 A — 室内 + 室外低空(飞行层 0.5–2.0 m)

| 层级 | 障碍半径范围 | 占地密度(稀疏→稠密) | 场景数建议 |
|------|-------------|---------------------|-----------|
| 小尺度 small | 0.1–0.5 m | 0.05–0.08 | 8 |
| 中尺度 medium | 0.5–1.5 m | 0.09–0.13 | 8 |
| 大尺度 large | 1.5–3.0 m | 0.12–0.18 | 14 |
| 混合 mixed | 0.1–3.0 m | 0.12–0.16 | 14 |

### 类 B — 室外高空(飞行层 10–13 m)

| 层级 | 障碍半径范围 | 占地密度(稀疏→稠密) | 场景数建议 |
|------|-------------|---------------------|-----------|
| 小尺度 small | 1.0–2.0 m | 0.05–0.08 | 8 |
| 中尺度 medium | 2.0–3.5 m | 0.09–0.13 | 8 |
| 大尺度 large | 3.5–6.0 m | 0.12–0.18 | 14 |
| 混合 mixed | 1.0–6.0 m | 0.12–0.16 | 14 |

> 每层多种场景(稀疏→稠密),沿用现有 scene_parallel 的
> `level_radius_min/max + level_occupancy_min/max` 机制,分两类各跑一轮。

---

## 2. 高度随机但定高(2.5D)

- **每任务一个随机高度,起终点同高**: `BlueprintTask.flight_height_m` 已是
  per-task 字段(默认 2.0,生成器未随机化)。类 A 在 [0.5,2.0] 均匀随机,
  类 B 在 [10,13] 均匀随机;单任务内 start/goal 高度 = 该任务高度。
- **障碍是 scene 级竖直圆柱,贯穿整层**: 障碍中心 z 按"类的飞行层中心"
  放置(类 A 中心 1.25m、类 B 中心 11.5m),`height_m` 设到贯穿整层:
  - 类 A: `obstacle_height_m ≈ 4 m`(覆盖 0–2.5 m)
  - 类 B: `obstacle_height_m ≈ 15 m`(覆盖 4.5–19.5 m)
  → 同一 scene 的障碍对层内任意高度任务都呈现相同水平截面(半径 r)。
- **改动点**: il_manager 放置障碍时用"scene 飞行层中心"而非全局
  `flight_height_m`;生成器 per-task 随机 `flight_height_m`。

---

## 3. 已知障碍(点云固定结构)当普通障碍

- 点云按飞行层切片 → 2D 占据图 → 作为**普通障碍占据格**,与动态圆柱一视同仁:
  - 障碍生成: 避开占据格(重叠拒绝)。
  - 任务起终点: 避开占据格 + 动态圆柱。
  - 连通检查: 占据图 ∪ 动态圆柱上跑 A*。
  - 专家: 通过深度观测(Unity 渲染自动包含固定结构)正常绕行,无特殊语义。
- **不需要**"不可穿透硬障碍"标记,不需要把固定结构转换为圆柱注入专家;
  固定结构仅影响 blueprint 生成侧(简化 A*/truth 的占据图)。

---

## 4. 起点在区域内 + 边界内缩

- 起终点采样严格在所属区域内部: 室内任务在**仓库子区域** `[-7,10]×[0,30]`
  内,室外任务在**工业子区域**内;都带 drone_radius 余量避开障碍。
- 障碍生成在整个区域进行,但**生成边界向内缩 margin**(沿用
  `boundary_margin_m` 语义,建议 1.2 m),避免障碍贴区域边界。

---

## 5. region 统一(两场景坐标不同,需决策)

- 场景 1(仓库)可飞区 `[-7,10]×[0,30]`,场景 0(工业)点云 ±25 m。
- 专家 region 是**全局一次 configure**,合并数据集后需要覆盖两场景。
  - **方案 A(推荐)**: 统一大 region = 两场景可飞区并集,专家 ESDF 历史图
    为稀疏网格(只填充观测格),内存开销可接受;任务生成按"子区域"约束
    起终点(室内→仓库子区域,室外→工业子区域)。
  - 方案 B: per-scene re-configure region,改动较大。
- 待确认: 统一 region 边界(建议 `x∈[-25,10], y∈[-25,30]`),以及
  工业子区域具体边界(避开大固定结构)。

---

## 6. 长中短距离 + 专家观测不变

- 距离分层沿用 `distance_short_max_m / distance_medium_max_m / ...`
  (short ≤8m / medium ≤16m / long >16m)。
- 专家观测**完全不变**: `fov 90°`、`range 5m`、`resolution 0.1m`。
  注: 高空 6m 半径障碍 > range 5m,在 5m 处几乎填满 FOV → 天然产生
  "必须宏观接管"的大障碍标签(这正是高空层要采集的样本)。

---

## 7. 一个数据集的合并方式

- 单一 output_dir(如 `il_data_joint_v2_col_5`),内含:
  - 类 A 任务(室内 scene 1 + 室外低空 scene 0)
  - 类 B 任务(室外高空 scene 0)
- episode/scene 用 `scene_id` 与新增 `region_class`(A/B)区分;
  manifest 记录每 task 的 `flight_height_m`、`region_class`、层级。

---

## 8. 分阶段实施(修订)

1. **阶段 0 — 点云基准化**: 导出 scene 0/1 点云 → 按层切片 → 两类占据图 +
   已知障碍格;输出俯视图确认子区域边界。
2. **阶段 1 — 生成器扩展**: (a) per-task 随机 flight_height_m;
   (b) scene 飞行层中心 + 贯穿高度障碍; (c) 已知障碍占据图避让;
   (d) 子区域采样 + 边界内缩。单元验证。
3. **阶段 2 — 类 A 试点**: 室内+低空 4 层级小规模(每层 2 场景),blueprint
   预检 + 采集验证专家轨迹/标签。
4. **阶段 3 — 类 B 高空试点**: 1–6m 障碍 + 12m 飞行,先单点验证深度可见性,
   再小规模采集。
5. **阶段 4 — 合并全量**: 两类 4 层级全量 + top-up,产出 col_5。

---

## 9. 待你最终确认的决策点

1. **统一 region 边界**(方案 A): `x∈[-25,10], y∈[-25,30]` 是否可接受?
   或给出你希望的边界。
2. **工业子区域边界**(室外低空/高空起终点与障碍生成范围): 建议
   低空 `[-20,20]×[-5,20]`、高空 `[-20,20]×[-20,20]`,待你确认。
3. **每层场景数**: 类 A/类 B 各 8/8/14/14(共 44 场景/类,88 场景)是否
   合适,还是沿用现有 10 场景/层。
4. **任务高度分布**: 均匀随机 [0.5,2.0] 与 [10,13],还是分层采样
   (如低空分 0.5/1.0/1.5/2.0 几档)?
5. **障碍高度**: 类 A 用 4m、类 B 用 15m(贯穿整层),是否认可?
6. **宏观接管配额**: 高空层是否提高 NORMAL/TURN 目标(大障碍绕行是核心样本)?
